#pragma once

#include "SpehsEngine/Net/Endpoint.h"
#include <memory>

/*
namespace se
{
	namespace net
	{
		class IOService;
		struct ConnectionSignaling;

		class SignalingClient
		{
		public:
			struct Impl;
			SignalingClient(IOService& ioService, const Endpoint &serverEndpoint);
			~SignalingClient();

			void update();

		private:
			friend struct ConnectionSignaling;
			std::unique_ptr<Impl> impl;
		};
	}
}
*/
